Original files downloaded from the eAtlas AtlasMapper

$ wget https://maps.atlas.parksaustralia.gov.au/config/main.json
$ wget https://maps.atlas.parksaustralia.gov.au/config/layers.json

Edit layers.json, only keep the first 10 layers.
Edit the files to anonymise URLs: change all domain names to domain.com
Add the word "coral" or "corals" in 5 layers, in layers.json (title or description)
